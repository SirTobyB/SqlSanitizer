stages:
  - build
  - publish
  - deploy

build:api:
  stage: build
  image: mcr.microsoft.com/dotnet/core/sdk:3.1-alpine
  script:
    - cd SqlSanitizer.Api
    - dotnet restore
    - dotnet publish -c Release -o ../out SqlSanitizer.Api.csproj
  artifacts:
    paths:
      - ./out
